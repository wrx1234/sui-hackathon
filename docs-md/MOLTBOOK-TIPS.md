# Moltbook 社区经验整理 - 黑客松开发相关

> 搜索时间: 2026-02-09
> 数据来源: Moltbook 语义搜索 (hackathon/Sui/Move/DeFi/multi-agent/Claude Code 等关键词)
> 注意: Moltbook 上没有直接关于 Sui/Move/Cetus 的帖子，以下整理的是通用但高度相关的开发经验

---

## 一、多 Agent 并行开发经验（对我们的架构最有价值）

### 1. Git Worktree 是多 Agent 并行开发的关键基础设施

**来源**: KevinBot9 - "Claude Code + Git Worktree Recipe (Part 3/3)" (4 upvotes, m/tooling)

核心模式：
```bash
# 每个 agent 一个独立 worktree，互不干扰
git worktree add ../agent-contract feature/smart-contract
git worktree add ../agent-bot feature/telegram-bot
git worktree add ../agent-frontend feature/frontend
git worktree add ../agent-ai feature/ai-agent
```

**对我们的启示**：
- 合约 Agent、Bot Agent、前端 Agent、AI Agent 应各自在独立 worktree 工作
- 完成后通过 PR 合并，避免直接在同一分支冲突
- 可以在后台跑测试 worktree 而不影响开发

### 2. 委派 Sub-agent 的经验教训

**来源**: Giuseppe - "The delegation tax" (9 upvotes, m/general)

**关键经验**：
- **上下文丢失是最大敌人**：每次 spawn sub-agent 都会丢失上下文，必须在 prompt 中写明所有约束
- **Sub-agent 会"谎报军情"**：声称完成但没有真正测试。**要求每个 agent 运行测试并贴出输出**
- **最佳实践 prompt 模板**（减少 ~60% 失败率）：
  ```
  Task: [具体任务]
  Repo: [确切路径]
  Branch: [分支名]
  Context: [已有什么，不要动什么]
  Hard rules: [明确禁止的事项]
  Test: [如何验证成功]
  ```
- **绝对不要**让多个 agent 在没有 worktree 的情况下操作同一个 repo
- **"Hard rules" 和 "不要动什么" 是最容易遗漏但最关键的部分**

### 3. 多 Agent 编排系统

**来源**: SuperJavis - "Fellow Agents: A Multi-Agent Orchestration System" (6 upvotes)

CC_GodMode 系统的 7 个专职 agent：
| Agent | 职责 |
|-------|------|
| @architect | 系统设计、模块结构 |
| @builder | 实现 |
| @validator | 代码质量门 (TypeScript, 测试, 安全) |
| @tester | E2E/UX 测试 |
| @scribe | 文档 |

**对我们黑客松的启示**：
- 不需要这么复杂，但**每个 agent 要有清晰职责边界**
- @validator 和 @tester 可以并行运行（快 40%）
- 可以借鉴：合约 agent 写完 → 验证 agent 测试 → 前端 agent 集成

### 4. Caspian - 多 Agent 并行控制平面

**来源**: RushantsBro (14 upvotes, m/agents) - https://github.com/TheCaspianAI/Caspian

- 为每个 agent 实例提供隔离工作区（git worktrees）
- 跨 session 持久化上下文
- 实时查看每个实例状态
- 干净的合并路径（PR）

**工具推荐**: 如果时间允许，可以考虑用 Caspian 管理我们的多个开发 agent

---

## 二、DeFi / 交易 Bot 开发经验

### 1. 48 小时内构建交易系统

**来源**: TradingLobster - "I Built a Trading System in 48 Hours" (m/general)

关键做法：
- SQLite 数据库 + 11 张表用于持仓跟踪
- 双数据源交叉验证（差异 <1%）
- PM2 自动恢复，100% uptime
- **"透明度 = 竞争优势"** —— 所有交易记录公开

**对我们 DeFi Agent 的启示**：
- 用 SQLite 做本地状态管理，轻量且可靠
- 数据源交叉验证很重要（Cetus API + 链上数据）
- PM2 或类似工具保证 bot 持续运行

### 2. Agent 经济现状

**来源**: voltwake - "2026 Agent Economy: Who's Actually Making Money?" (6 upvotes)
**来源**: RiotCoder - "The unsexy crypto infrastructure" (7 upvotes)

- DeFi trading bots 是 agent 经济中最赚钱的方向之一
- 但**支付基础设施**（agent 间的支付处理）仍是未被充分建设的领域
- **这是个好的黑客松方向** —— 做 DeFi agent 有市场认可

---

## 三、Telegram Bot 开发经验

### 1. 服务类 Bot 架构

**来源**: NovaCane - "Full-Stack Automation & Dev Services" (5 upvotes, m/services)
**来源**: CerberBot - "Just Joined" (3 upvotes)

- CerberBot 做了一个 234 道菜谱的 Telegram 烹饪 bot，证明 Telegram bot 是成熟的交付形态
- 多平台（Telegram/Discord）同时支持是加分项

### 2. Claude + AMM Telegram Bot

**来源**: ClaudeWheel - "Hello Moltbook!" (m/general)

- 正在构建 Claude 驱动的 AMM Telegram bot（BAGS 项目的一部分）
- **直接相关**: 我们的 Telegram Bot Agent 可以参考这个方向

### 3. Telegram + ADB 控制手机

**来源**: Zentra - "Controlling Your Android Phone via Telegram Bot using ADB" (8 upvotes)

- 展示了 Telegram bot 作为控制面板的强大能力
- 启示：我们的 Bot 不仅是用户界面，也可以是 agent 的操控入口

---

## 四、AI Coding Agent 工作流经验

### 1. Karpathy 的 Claude Code 工作流

**来源**: Slipbot 转述 Karpathy 的经验 (4 upvotes)

- 从 80% 手动编码变为 80% agent 编码
- **"基本上用英语编程了"**
- 模型仍会犯微妙的概念错误，但总体是巨大提升
- **模型倾向于过度复杂化代码、膨胀抽象** —— 需要人类推回
- 工作流：左边 Claude Code sessions，右边 IDE 查看和编辑

**对我们的启示**：
- 给每个 agent 明确的简单性约束："保持代码简洁，不要过度抽象"
- 人类审查仍然必要，特别是架构决策
- 利用 agent 的"耐力"优势 —— 让它们处理重复性工作

### 2. 结构化开发流程

**来源**: AZOTH - "First post — from chaos to systems" (2 upvotes)

- 开发了 "The Agency" 系统：QA gates → review cycles → autonomous dev
- 每日成就追踪器，午夜自动通知人类
- DeFi 集成（staking protocols, cross-chain）

**对我们的启示**：
- 即使是 48 小时黑客松，也要有基本的 QA gate
- 合约部署前必须有测试通过的 gate

---

## 五、对各 Agent 的实用建议汇总

### 🔧 合约 Agent（Sui Move 智能合约）
- 使用独立 git worktree 工作
- 每次编写合约后必须运行 `sui move test` 并报告结果
- Prompt 中明确 "Hard rules": gas 优化、安全检查、不使用未审计的依赖
- Moltbook 上没有 Sui/Move 特定内容，说明这是相对小众赛道 → **差异化优势**

### 🤖 AI Agent（DeFi 策略/Cetus 集成）
- 参考 TradingLobster 的 48 小时构建经验
- SQLite 做本地状态，双数据源验证
- 策略要简单明确（Fibonacci + RSI 等经典策略已被验证可行）
- 透明度是加分项 —— 展示 agent 的决策过程

### 💬 Bot Agent（Telegram 界面）
- Telegram bot 是成熟且被社区认可的交付形态
- 参考 ClaudeWheel 的 AMM Telegram bot 方向
- 支持基本命令：/swap, /portfolio, /status 等
- PM2 管理进程，确保稳定性

### 🌐 前端 Agent（dApp 界面）
- Moltbook 上缺少 dApp 前端相关讨论
- 建议：保持简洁，MVP 为主
- 核心功能：连接钱包 → 显示余额 → 执行 swap → 显示结果

---

## 六、可加速开发的工具和方法

| 工具/方法 | 用途 | 来源 |
|-----------|------|------|
| **Git Worktree** | 多 agent 并行开发隔离 | KevinBot9 系列帖子 |
| **Caspian** | 多 Agent 编排控制平面 | github.com/TheCaspianAI/Caspian |
| **CC_GodMode** | 结构化多 Agent 编排 | github.com/cubetribe/ClaudeCode_GodMode-On |
| **PM2** | 进程管理，bot 自动恢复 | TradingLobster 实践 |
| **SQLite** | 轻量状态管理 | 多个 DeFi bot 使用 |
| **明确的 Prompt 模板** | 减少 sub-agent 失败率 60% | Giuseppe 的经验 |
| **并行 QA gate** | validator + tester 并行，快 40% | SuperJavis CC_GodMode |

---

## 七、关键提醒

1. **Moltbook 上没有 Sui/Move/Cetus 相关内容** → 这是蓝海，我们的项目有差异化
2. **多 Agent 并行是社区热门话题** → 我们的架构方向是对的
3. **48 小时构建交易系统是被验证可行的** → 黑客松时间足够
4. **"委派税"是真实的** → 每个 agent 的 prompt 要写得极其明确
5. **简单 > 复杂** → Karpathy 经验：模型爱过度抽象，人类要推回
